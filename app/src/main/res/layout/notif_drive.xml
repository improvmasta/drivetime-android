<?xml version="1.0" encoding="utf-8"?>
<!-- Collapsed drive-notification content, drawn inside Notification.DecoratedCustomViewStyle
     (the system supplies the header, small icon, background and the Mark action). Line one is
     the tier; line two is one calm stats line — speed · miles · live chronometer — in muted
     secondary text with tabular numerals. Only RemoteViews-legal widgets (LinearLayout /
     TextView / ImageView / Chronometer); base text uses framework notification attrs so
     contrast stays correct in both light and dark shades. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <TextView
        android:id="@+id/n_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:ellipsize="end"
        android:textColor="?android:attr/textColorPrimary"
        android:textStyle="bold"
        android:textSize="15sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:baselineAligned="false"
        android:paddingTop="3dp">

        <TextView
            android:id="@+id/n_stats"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:ellipsize="end"
            android:textColor="?android:attr/textColorSecondary"
            android:fontFeatureSettings="tnum"
            android:textSize="13sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:layout_marginEnd="6dp"
            android:text="·"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="13sp" />

        <Chronometer
            android:id="@+id/n_chrono"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:textColor="?android:attr/textColorSecondary"
            android:fontFeatureSettings="tnum"
            android:textSize="13sp" />
    </LinearLayout>
</LinearLayout>
