name: Build APK

on:
  push:
    # `compile` (below) runs on EVERY branch; `build` gates itself back to main. There is no JDK or
    # Android SDK on the dev host, so CI is the only Kotlin compiler that exists — and until now it
    # only ran on main, which meant a work branch had no compiler at all and a syntax error was
    # found by *shipping*. See the compile job.
    branches: ["**"]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ---------------------------------------------------------------------------------------------
  # The fast one (HARDENING.md 5.6). Compiles the Kotlin — app sources AND unit-test sources — and
  # nothing else: no lint, no APK, no AAB, no Play upload. It exists because the full `build` below
  # takes many minutes, and the dev host cannot compile Kotlin at all, so the cost of a typo used
  # to be a whole build matrix. This fails in ~2 minutes instead.
  #
  # It runs on every branch and every PR, so a work-in-progress branch gets a compiler.
  # ---------------------------------------------------------------------------------------------
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      - name: Generate Gradle wrapper
        run: gradle wrapper --gradle-version 8.11.1

      # Test sources too: they are Kotlin, they break the same way, and a test that doesn't compile
      # is indistinguishable from one that doesn't exist.
      - name: Compile Kotlin (app + unit tests)
        run: ./gradlew compileGithubDebugKotlin compileGithubDebugUnitTestKotlin --stacktrace

  build:
    # The expensive path — tests, lint, both artifacts, the GitHub release, the Play upload.
    #
    # Everything it used to run on, it still runs on: pushes to main, PRs into main, and manual
    # dispatch. The ONLY thing that changed is that a push to some other branch no longer drops
    # through to it — that push now gets `compile` and stops. (Keep `pull_request` in this list:
    # without it a PR's ref is refs/pull/N/merge, so a branch-name test alone would silently stop
    # running the tests and lint on every PR.)
    if: >-
      github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch' ||
      github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    # Read-only: nothing here writes to the repo any more. The step that needed `contents: write`
    # cut a GitHub release carrying the sideload APK + version.json for the in-app updater; the
    # updater is deleted and that release is retired with it. The debug APK is still produced —
    # as a workflow artifact (`drivetime-debug-apk`), downloadable from the run — so there is
    # still a hand-installable build; it is just no longer published anywhere.
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.11.1"

      # No wrapper jar is committed; generate it, then build. Gradle 8.11.1 is AGP 8.10's
      # floor — keep this in step with the AGP version in build.gradle.kts.
      - name: Generate Gradle wrapper
        run: gradle wrapper --gradle-version 8.11.1

      # Flavours (github | play) rename every task: testDebugUnitTest is now
      # testGithubDebugUnitTest, etc. The two flavours share all source, so testing one
      # covers both — except the updater gate, which is a compile-time constant.
      - name: Unit tests
        run: ./gradlew testGithubDebugUnitTest --stacktrace

      - name: Android Lint
        run: ./gradlew lintGithubDebug --stacktrace

      # --- sideload channel: the signed debug APK, as before ---
      - name: Build sideload APK
        run: ./gradlew assembleGithubDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: drivetime-debug-apk
          path: app/build/outputs/apk/github/debug/*.apk
          if-no-files-found: error

      # --- Play channel: a release AAB, which is the only format Play accepts ---
      # Signed with the same committed key, which here acts as the UPLOAD key. Whether the
      # APK Play then serves is signed with this key too depends on the app-signing choice
      # made in Play Console — and that decision is load-bearing (CLAUDE.md → Distribution).
      - name: Build Play bundle
        run: ./gradlew bundlePlayRelease --stacktrace

      - name: Upload AAB
        uses: actions/upload-artifact@v4
        with:
          name: drivetime-play-aab
          path: app/build/outputs/bundle/playRelease/*.aab
          if-no-files-found: error

      # There is no "publish a GitHub release" step here any more, and re-adding one would be a
      # step backwards. It existed to feed the in-app updater (which polled
      # releases/latest/download/version.json) — and the updater is deleted, because Play's
      # Device and Network Abuse policy forbids an app updating itself by any route but Play.
      # With the updater gone, the release published a version.json nobody read and an APK
      # nobody was served. The `drivetime-debug-apk` artifact above still gives you a
      # hand-installable build off any run. Play is the only channel.

      # --- Play channel: push the AAB straight to the internal testing track ---
      # Gated on push-to-main exactly like the release above, and NEVER on pull_request: this
      # repo is public, and a fork's PR must not get within reach of the publisher credential.
      # Skipped (not failed) when the secret is absent, so a fork — or this repo before the
      # secret was set — still builds green.
      #
      # Play rejects a versionCode it has already seen, so the monotonic CI run number that
      # feeds versionCode (app/build.gradle.kts) is load-bearing here, not a convenience.
      # The `secrets` context is NOT available in a step-level `if`, so the presence of the
      # credential is resolved here into a plain output the next step can branch on. It also
      # writes the release note Play shows testers (the commit subject, capped at Play's 500).
      - name: Stage Play release
        id: play
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          PLAY_SA: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          mkdir -p distribution/whatsnew
          git log -1 --pretty=%s | cut -c1-500 > distribution/whatsnew/whatsnew-en-US
          if [ -n "$PLAY_SA" ]; then
            echo "ready=true" >> "$GITHUB_OUTPUT"
          else
            echo "ready=false" >> "$GITHUB_OUTPUT"
            echo "::notice::PLAY_SERVICE_ACCOUNT_JSON is not set — skipping the Play upload."
          fi

      - name: Upload to Play (internal testing)
        if: steps.play.outputs.ready == 'true'
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
          packageName: org.jupiterns.drivetime
          releaseFiles: app/build/outputs/bundle/playRelease/*.aab
          track: internal
          status: completed
          whatsNewDirectory: distribution/whatsnew

      - name: Upload lint report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: app/build/reports/lint-results-githubDebug.html
          if-no-files-found: ignore

      - name: Upload unit-test report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-report
          path: app/build/reports/tests/testGithubDebugUnitTest
          if-no-files-found: ignore
